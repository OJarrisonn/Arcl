main {
    decl var Int age = 19;
    age = 20;

    age >= 15;

    if age >= 21 ? print "Drink"
     | age >= 18 ? print "Can't drink yet"
     | age >= 16 ? print "Drive"
     else print "No rights, shut up kid";
}